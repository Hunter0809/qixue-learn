# Dictionary Data

Place `ecdict.csv` in this directory to enable local dictionary document search.

Recommended source: `skywind3000/ECDICT`, MIT licensed English-Chinese dictionary database:
https://github.com/skywind3000/ECDICT

The lookup algorithm scans the CSV document directly and returns exact matches before prefix matches.
